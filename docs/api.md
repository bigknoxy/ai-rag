# API Docs

- POST /api/ingest: Ingest batch of documents. Request body per `specs/001-create-a-feature/contracts/ingest_openapi.yaml`.
- GET /api/health: Health check endpoint returning `status: healthy`.
